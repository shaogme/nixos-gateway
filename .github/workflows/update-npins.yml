name: Auto Update npins

on:
  schedule:
    - cron: '30 19 * * *' # 03:30 Beijing Time (UTC+8)
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  pull-requests: write

jobs:
  update-npins:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Install Nix
        uses: cachix/install-nix-action@master
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Update npins dependencies
        run: |
          echo "Updating npins in root..."
          nix shell nixpkgs#npins -c npins upgrade
          nix shell nixpkgs#npins -c npins update

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.PAT }}
          commit-message: "chore(deps): update npins dependencies"
          committer: GitHub <noreply@github.com>
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          signoff: false
          branch: auto-update-npins
          delete-branch: true
          title: "chore(deps): update npins dependencies"
          body: |
            Automated npins dependency update.
            
            This PR updates the `npins/sources.json` in the repository that uses `npins`.
            
            Generated by GitHub Actions.
          labels: |
            dependencies
            automated

      - name: Enable Auto-Merge
        if: steps.cpr.outputs.pull-request-operation == 'created'
        run: gh pr merge --auto --merge "${{ steps.cpr.outputs.pull-request-number }}"
        env:
          GH_TOKEN: ${{ secrets.PAT }}
